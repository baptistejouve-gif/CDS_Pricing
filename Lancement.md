# Lancer l'application

1. Ouvrir un environnement Python (VS Code, Anaconda Prompt ou Terminal).

2. Installer les dépendances :
 
pip install -r requirements.txt

3. Lancer l'application :

streamlit run app.py
